---
title: "LLM Adapter 开发规范"
source:
  - "modules/llm-adapter/INTERFACE.md"
  - "docs/model-provider-config-table.md"
  - "docs/model-capability-table-verified-2026-05-31.md"
  - "docs/context-compact-and-mcp-cli-impl-plan-2026-05-30.md"
  - "docs/gemma4-12b-local-deploy-2026-06-15.md"
  - "docs/coolzhu-model-finetune-plan-2026-06-16.md"
  - "docs/development-standard.md"
status: "migrated"
migrated_at: "2026-06-19T00:00:00+08:00"
---
# LLM Adapter 开发规范

## 模块开发规范

- Provider key、base_url、model alias 解析不得泄漏到 core/gui。
- 新增 provider 必须进入 ProviderKind/ModelRegistry，并保留 DTO 向后兼容。
- Custom provider 的 base_url/endpoint 只在 Custom 场景生效，避免非 Custom 误走兼容接口。
- 工具调用输出优先结构化 content block，provider 不支持时由 adapter 降级。

## 项目级通用规范

- 新增业务配置进入 `coolzhu.toml` 或模块配置文件；不得扩散 `COOLZHU_*` / `CLAW_*` 自定义业务环境变量。
- GUI 只做 API client，不直接承载 core-runtime、vision、computer-use 的业务细节。
- 真实键鼠输入默认关闭，必须有显式授权、dry-run/证据链和审计。
- 跨模块接口变更先改对应 `INTERFACE.md`，删除字段必须先 deprecated。
- 用户可见功能验收必须启动真实前端，并观察可见反馈；HTTP/API 测试只能作辅助证据。
- 修改高风险链路时按规范备份、设置命令超时，并把验证结果写入 work-log。

## 验证原则

- 修改后运行对应模块的 `cargo check/test --offline`；用户可见功能还要真实前端或截图证据。
- 工作日志记录：修改目的、涉及模块、验证命令、结果、风险与回滚方式。
