---
title: "Core Runtime 开发规范"
source:
  - "modules/core-runtime/INTERFACE.md"
  - "docs/session-memory-workspace-integration-plan-2026-05-10.md"
  - "docs/context-management-analysis-2026-05-30.md"
  - "docs/context-compaction-threshold-benchmark-2026-05-31.md"
  - "docs/code-audit-context-tools-goal-2026-05-31.md"
  - "docs/work-logs/2026-06-07-video-multiround-memory.md"
  - "docs/development-standard.md"
status: "migrated"
migrated_at: "2026-06-19T00:00:00+08:00"
---
# Core Runtime 开发规范

## 模块开发规范

- core-runtime 不依赖 GUI、桌面输入或具体视觉实现。
- 修改 Session/ConversationMessage/工具调用结构必须同步接口契约与 GUI 集成测试。
- 上下文预算与记忆注入要按模型能力表动态推导，避免固定窗口截断。
- 配置读取遵循 workspace active path，避免全局状态污染。

## 项目级通用规范

- 新增业务配置进入 `coolzhu.toml` 或模块配置文件；不得扩散 `COOLZHU_*` / `CLAW_*` 自定义业务环境变量。
- GUI 只做 API client，不直接承载 core-runtime、vision、computer-use 的业务细节。
- 真实键鼠输入默认关闭，必须有显式授权、dry-run/证据链和审计。
- 跨模块接口变更先改对应 `INTERFACE.md`，删除字段必须先 deprecated。
- 用户可见功能验收必须启动真实前端，并观察可见反馈；HTTP/API 测试只能作辅助证据。
- 修改高风险链路时按规范备份、设置命令超时，并把验证结果写入 work-log。

## 验证原则

- 修改后运行对应模块的 `cargo check/test --offline`；用户可见功能还要真实前端或截图证据。
- 工作日志记录：修改目的、涉及模块、验证命令、结果、风险与回滚方式。
