---
title: "GUI Desktop / 桌宠 开发规范"
source:
  - "modules/gui-desktop/INTERFACE.md"
  - "docs/desktop-pet-clawd-integration-and-new-features-2026-05-31.md"
  - "docs/desktop-pet-review-and-filedrop-2026-06-04.md"
  - "docs/clawd-on-desk-port-analysis-2026-05-31.md"
  - "docs/work-logs/2026-06-18-pet-settings-session-protocol-agent-reach.md"
  - "docs/development-standard.md"
status: "migrated"
migrated_at: "2026-06-19T00:00:00+08:00"
---
# GUI Desktop / 桌宠 开发规范

## 模块开发规范

- 桌宠资源修复禁止非等比拉伸；优先用帧选择、透明边距、画布定位和锚点修正。
- `pet-theme.json` 与 `pet-mini.html` 的 frameSets 要同步，避免状态切换但无帧显示。
- 桌面壳不得继续承载 core/computer-use 业务细节，迁移时保留安全测试。
- Tauri shell/Web 后端/静态资源路径必须配置化，避免安装包路径硬编码。

## 项目级通用规范

- 新增业务配置进入 `coolzhu.toml` 或模块配置文件；不得扩散 `COOLZHU_*` / `CLAW_*` 自定义业务环境变量。
- GUI 只做 API client，不直接承载 core-runtime、vision、computer-use 的业务细节。
- 真实键鼠输入默认关闭，必须有显式授权、dry-run/证据链和审计。
- 跨模块接口变更先改对应 `INTERFACE.md`，删除字段必须先 deprecated。
- 用户可见功能验收必须启动真实前端，并观察可见反馈；HTTP/API 测试只能作辅助证据。
- 修改高风险链路时按规范备份、设置命令超时，并把验证结果写入 work-log。

## 验证原则

- 修改后运行对应模块的 `cargo check/test --offline`；用户可见功能还要真实前端或截图证据。
- 工作日志记录：修改目的、涉及模块、验证命令、结果、风险与回滚方式。
