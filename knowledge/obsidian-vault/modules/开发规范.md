---
title: "modules 一级目录开发规范"
source:
  - "docs/module-completion-status.md"
  - "docs/interface-contracts.md"
  - "docs/repository-structure.md"
  - "docs/requirements-management.md"
  - "modules/gui-web/INTERFACE.md"
  - "modules/gui-desktop/INTERFACE.md"
  - "modules/computer-use/INTERFACE.md"
  - "modules/vision/INTERFACE.md"
  - "modules/llm-adapter/INTERFACE.md"
  - "modules/tooling/INTERFACE.md"
  - "modules/core-runtime/INTERFACE.md"
  - "modules/diagnostics/INTERFACE.md"
  - "modules/cli/INTERFACE.md"
status: "migrated"
migrated_at: "2026-06-19T00:00:00+08:00"
---
# modules 聚合开发规范

## 模块开发规范

- 模块内部独立迭代，跨模块只走稳定接口。
- 新增模块能力时优先更新对应模块 vault 文档和 `INTERFACE.md`。
- 跨模块联调测试放根目录 `tests/`，模块内测试留在 package 内。

## 项目级通用规范

- 新增业务配置进入 `coolzhu.toml` 或模块配置文件；不得扩散 `COOLZHU_*` / `CLAW_*` 自定义业务环境变量。
- GUI 只做 API client，不直接承载 core-runtime、vision、computer-use 的业务细节。
- 真实键鼠输入默认关闭，必须有显式授权、dry-run/证据链和审计。
- 跨模块接口变更先改对应 `INTERFACE.md`，删除字段必须先 deprecated。
- 用户可见功能验收必须启动真实前端，并观察可见反馈；HTTP/API 测试只能作辅助证据。
- 修改高风险链路时按规范备份、设置命令超时，并把验证结果写入 work-log。

## 验证原则

- 修改后运行对应模块的 `cargo check/test --offline`；用户可见功能还要真实前端或截图证据。
- 工作日志记录：修改目的、涉及模块、验证命令、结果、风险与回滚方式。
