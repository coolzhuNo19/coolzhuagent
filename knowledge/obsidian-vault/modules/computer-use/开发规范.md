---
title: "Computer Use 开发规范"
source:
  - "modules/computer-use/INTERFACE.md"
  - "docs/precise-click-grounding-plan-2026-05-10.md"
  - "docs/grounding-router-verification-plan.md"
  - "docs/work-logs/2026-06-19-computer-use-plugin-fix.md"
  - "docs/work-logs/2026-05-10-verified-click-pipeline.md"
  - "docs/development-standard.md"
status: "migrated"
migrated_at: "2026-06-19T00:00:00+08:00"
---
# Computer Use 开发规范

## 模块开发规范

- 新增鼠标/键盘动作必须补 CLI 或 Aimlab 类交互回归。
- 修改坐标映射必须覆盖分辨率、DPI 缩放和多显示器风险。
- 真实执行前必须通过权限闸门、视觉确认或安全靶场，不允许默认真实输入。
- 输入后端策略变更同时验证 sendinput 和 interception 成功/失败路径。

## 项目级通用规范

- 新增业务配置进入 `coolzhu.toml` 或模块配置文件；不得扩散 `COOLZHU_*` / `CLAW_*` 自定义业务环境变量。
- GUI 只做 API client，不直接承载 core-runtime、vision、computer-use 的业务细节。
- 真实键鼠输入默认关闭，必须有显式授权、dry-run/证据链和审计。
- 跨模块接口变更先改对应 `INTERFACE.md`，删除字段必须先 deprecated。
- 用户可见功能验收必须启动真实前端，并观察可见反馈；HTTP/API 测试只能作辅助证据。
- 修改高风险链路时按规范备份、设置命令超时，并把验证结果写入 work-log。

## 验证原则

- 修改后运行对应模块的 `cargo check/test --offline`；用户可见功能还要真实前端或截图证据。
- 工作日志记录：修改目的、涉及模块、验证命令、结果、风险与回滚方式。
