# 2026-05-05 需求管理梯队重排

## 背景

根据最新需求管理规则，重新整理 `docs/requirements-management.md`：

- Web-GUI 卡片功能接入作为第一梯队。
- Core Runtime 与分层 beads 记忆系统作为第二梯队。
- Computer Use 与内视觉作为第三梯队。
- 其它模块按依赖关系、用户价值和交互风险重新排序。
- 只剩真实窗口、硬件或人工操作确认的事项单独标记为 `待交互验证`。

## 调整结果

- 新增模块梯队规则与依赖提升规则。
- 将 LLM provider 诊断、结构化 tool call、工具目录/dispatch、Diagnostics、Audio 状态 API 归入第一梯队支撑依赖。
- 将 `REQ-MEM-002/004/005` 标为第一梯队联调前置依赖，因为回复引用、跨 agent 转发和 prompt preview 依赖这些能力。
- 将 WebView/浏览器显示一致性、桌宠真实窗口拉起/双击/气泡、本地 VLM 命中率确认标记为 `待交互验证`。
- 更新下一步计划为按梯队推进，而不是按旧模块顺序推进。

## 统计

当前需求总数：67。

状态分布：

- 已完成：15
- 测试中：21
- 待交互验证：5
- 开发中：3
- 待开发：22
- 暂停：1

优先级分布：

- P0：7
- P1：50
- P2：8
- P3：2

## 验证

- 已用 PowerShell 检查需求状态统计与文档统计一致。
- 已确认文档保存为 UTF-8 with BOM，便于 Windows PowerShell 正常显示中文。
