# 2026-08-24：0.2.8 安装包、图标与隔离运行收尾自检

## 范围

本轮针对用户反馈的桌面图标问题完成最终构建验证，并用新生成的 0.2.8 安装包内容进行隔离启动自检。没有覆盖用户当前已安装的 0.2.7 实例，避免改变正在使用的 8765 会话；安装包本身和包内二进制均已验证。

## 构建产物

- 构建命令：`./scripts/build-msi.ps1 -Version 0.2.8 -Configuration debug`
- MSI：`dist/CoolzhuAgent-0.2.8.msi`
- SHA256：`66E0925E3481336CAF67F154CCC703C0F346065ED58212C4179F149E8744DE1A`
- WiX：5.0.2+aa65968c
- 签名：unsigned（当前设备没有配置发布签名证书）
- 包清单测试：`PASS package-manifest`
- 包安全测试：`PASS package-safety`，791 个文件，`findings=[]`

## 图标验证

本轮分别核验了资源、PE 与 MSI 数据库三层，避免只看资源文件而遗漏快捷方式回退问题：

1. `coolzhu-application-icon.ico` 保留 16/24/32/48/64/128/256 七个 Shell 尺寸。
2. 从 `package/COOLZHU-AGENT.exe` 提取关联图标，得到带 `CZ` 的图标，和上轮修复后的嵌入图标 PNG SHA256 一致。
3. 只读查询 0.2.8 MSI 的 `Icon` 表得到 `CoolzhuInstallerIcon` 与 `CoolzhuApplicationIcon`；`StartMenuShortcut`、`DesktopShortcut` 均绑定 `CoolzhuApplicationIcon`。
4. 因此启动器 PE、WiX 快捷方式和安装器资源均不再依赖 Windows 的蓝白默认图标回退路径。

证据汇总：`tmp/package-0.2.8-selfcheck-evidence-round12.json`；图标预览：`tmp/package-0.2.8-launcher-icon.png`。

## 隔离启动自检

使用 `COOLZHU_RUNTIME_DIR=tmp/package-runtime-selfcheck-20260824-8802` 和 `127.0.0.1:8802` 启动包内 `package/bin/coolzhu-web-console.exe`：

- `/api/system/info`、`/api/diagnostics/health`、`/api/audio/status`、`/api/computer-use/capabilities`、`/api/showui/service`、`/api/realtime/session/status` 均返回 HTTP 200。
- 构建版本显示 `0e9bf1fc631d · 2026-08-23`，确认运行的是本轮代码。
- 健康检查为 `9 ok / 1 warn / 1 error`；唯一 error 是隔离运行目录没有模型凭据，属于预期配置提示，不是启动失败。
- 本机没有 ShowUI 本地模型：ShowUI shell 可由桌面壳管理但 backend `available=false`，computer-use 能力正确标记 `showui=skipped`；UIA、OCR/template、人工确认降级链路仍可用，真实输入保持关闭。
- 语音状态正确显示 STT 不可用、TTS 可用；realtime 维持 `half_duplex_guarded`，4 个 full-streaming readiness gate 为 0/4。
- 自检后确认 8802 端口、包内 web-console 与包内 tauri shell 均已退出。

界面证据：`tmp/package-0.2.8-ui-evidence-round12.png`；计算机使用降级界面证据：`tmp/package-0.2.8-computer-use-ui-evidence-round12.png`。

## 提交范围

本轮只新增本工作日志；用户工作区中未跟踪的 wuxia 图标与三栏 UI 设计资产保持原状，没有混入本轮发布提交。随后将工作日志与此前统一 PR 分支一起推送。
